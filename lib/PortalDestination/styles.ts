import makeStyles from "../styles/makeStyles";

const useStyles = makeStyles(
  {
    root: {
      position: "absolute",
      top: 0,
      left: 0,
      width: "100%"
    }
  },
  { name: "SonnatPortalDestination" }
);

export default useStyles;
