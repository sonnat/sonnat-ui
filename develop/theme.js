import createTheme from "@sonnat/ui/dist/styles/createTheme";

const theme = createTheme({
  typography: {
    ltrFontFamily: "Roboto",
    rtlFontFamily: "IRANSans"
  },
  direction: "ltr"
});

export default theme;
