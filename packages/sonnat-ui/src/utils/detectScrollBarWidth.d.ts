/**
 * Calculates the document's scroll-bar width.
 *
 * @function detectScrollBarWidth
 *
 * @return {number} Returns the scroll-bar width (unitless).
 */
export default function detectScrollBarWidth(): number;
