/**
 * @function getIconVariableFromProp
 *
 * @param {string} identifierProp The icon's identifier name.
 *
 * @return {string} Returns the font icon's U+ code.
 */
// eslint-disable-next-line no-unused-vars
export default function getIconVariableFromProp(identifierProp: string): string;
