/**
 * @function usePreviousValue
 *
 * @param {T} value The current value to be cached.
 * @returns Returns the previous cached value.
 */
/* eslint-disable */
export default function usePreviousValue<T>(value: T): (value: T) => T;
/* eslint-enable */
